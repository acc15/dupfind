#pragma once


template <typename String>
size_t max_myers_distance(const String& s1, const String& s2) {
    return s1.size() + s2.size();
}

template <typename String>
size_t myers_distance(const String& s1, const String& s2) {

    return 0;
}

